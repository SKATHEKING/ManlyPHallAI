# All dependencies are open-source and free. License summary:
# - FastAPI, LangChain, Ollama, discord.py: MIT
# - sentence-transformers, Chroma: Apache 2.0
# - uvicorn, pydantic: BSD
# - Other utilities: Permissive open-source licenses
# No proprietary or paid dependencies required.

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.8.0
# backend/config.py reads settings from the environment and .env through this.
pydantic-settings>=2.4.0
# Required by the UploadFile parameter on POST /ingest. FastAPI raises at
# route-definition time without it, so importing backend.main fails outright.
# Not imported by our code directly, which is why it is easy to miss.
python-multipart>=0.0.9

# Document Parsing
pypdf>=4.3.0
ebooklib==0.18.0
python-docx>=1.1.2

# Text Processing
# chunker.py imports langchain_text_splitters directly. Current langchain
# releases no longer pull that in transitively, so declare it explicitly.
# The langchain metapackage itself is not imported anywhere in this repo.
langchain-text-splitters>=0.2.0
sentence-transformers>=3.0.0
tokenizers>=0.19.0

# Vector Database
chromadb>=0.5.0

# LLM Integration
ollama>=0.3.0
requests>=2.32.0
discord.py>=2.4.0

# Data Processing
numpy>=2.0.0
pandas>=2.2.0

# Development & Testing
pytest>=8.0.0
python-dotenv>=1.0.1

# Utilities
click>=8.1.7
typer>=0.12.0
setuptools>=70.0.0
wheel>=0.43.0
